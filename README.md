# About NomNom Puzzle

# ⚠️ Clone project

### Firebase

We put Assets/Firebase folder into .gitignore to avoid git error volume folder exceed > 100MB
You should download Firebase SDK & import 2 packages: Firebase Analytics & Firebase Crashlytics into your local project
